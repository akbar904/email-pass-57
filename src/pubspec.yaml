name: com.example.simple_app
description: A simple Flutter application using the Cubit pattern for state management, featuring a login screen with email and password, and a home screen with a logout button.
version: 1.0.0
environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.5.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.0
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.0.0
  mocktail: ^0.3.0

flutter:
  uses-material-design: true